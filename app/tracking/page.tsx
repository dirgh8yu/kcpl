import { Container } from "../components/container";
import { PageShell } from "../components/page-shell";
import { company } from "../company-data";
import { createPageMetadata } from "../seo";
export const metadata = createPageMetadata({ title: "Shipment Tracking Support", description: "Contact the KCPL operations team with your shipment reference to request a current cargo status update.", path: "/tracking" });
export default function TrackingPage(){return <PageShell eyebrow="Shipment tracking" title="Follow your shipment journey." intro="For a current shipment update, contact the KCPL operations team with your shipment reference."><section className="section bg-offwhite"><Container><div className="mx-auto max-w-3xl bg-white p-7 shadow-[0_20px_60px_rgba(8,35,63,.08)] sm:p-12"><p className="eyebrow text-gold">Shipment support</p><h2 className="mt-4 text-3xl font-extrabold tracking-[-.04em] text-navy">Request a status update.</h2><p className="mt-6 max-w-xl text-base leading-8 text-slate">Include your KCPL shipment reference, company name and destination so the operations team can identify the movement.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><a href={`mailto:${company.email}?subject=${encodeURIComponent("KCPL shipment status request")}`} className="inline-flex min-h-14 items-center justify-center bg-navy px-7 text-xs font-bold uppercase tracking-[.15em] text-white">Email operations</a><a href="tel:+97714987510" className="inline-flex min-h-14 items-center justify-center border border-navy px-7 text-xs font-bold uppercase tracking-[.15em] text-navy">Call {company.phones[0]}</a></div></div></Container></section></PageShell>}
