<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- A payment's return page links back as kcpl://payment/<id>. -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kcpl" android:host="payment" />
            </intent-filter>
        </activity>
        <!-- The home screen widget: the shipment worth a glance. KCPL only;
             KCPL Ops has no widget. -->
        <receiver
            android:name=".KcplShipmentWidget"
            android:exported="false"
            android:label="@string/kcpl_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kcpl_shipment_widget" />
        </receiver>
    </application>
</manifest>
