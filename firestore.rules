rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // KCPL reads and writes operational data only through authenticated
    // Next.js server routes using the Firebase Admin SDK. Direct browser
    // access to Firestore is intentionally disabled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
